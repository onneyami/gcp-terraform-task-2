apiVersion: v1
kind: ConfigMap
metadata:
  name: jenkins-casc-config
  namespace: jenkins
data:
  jenkins.yaml: |
    jenkins:
      slaveAgentPort: 50000
      securityRealm:
        local:
          allowsSignup: false
          users:
            - id: "admin"
              password: "AdminPassword123!"
      authorizationStrategy:
        loggedInUsersCanDoAnything:
          allowAnonymousRead: false
      clouds:
        - kubernetes:
            name: "kubernetes"
            serverUrl: "https://kubernetes.default.svc"
            jenkinsUrl: "http://jenkins-service.jenkins.svc.cluster.local:8080"
            templates:
              - name: "gcp-agent"
                label: "gcp-agent"
                serviceAccount: "jenkins-sa"
                containers:
                  - name: "jnlp"
                    image: "jenkins/inbound-agent:latest"
                    workingDir: "/home/jenkins/agent"
                  - name: "build-tools"
                    image: "gcr.io/google.com/cloudsdktool/google-cloud-cli:slim"
                    command: "cat"
                    ttyEnabled: true

    credentials:
      system:
        domainCredentials:
          - credentials:
              - string:
                  scope: GLOBAL
                  id: "argocd-jenkins-token"
                  secret: "${ARGOCD_TOKEN}"
                  description: "ArgoCD API Token for Jenkins Pipeline"
              - usernamePassword:
                  scope: GLOBAL
                  id: "github-jenkins-token"
                  username: "onneyami"
                  password: "${GITHUB_TOKEN}"
                  description: "GitHub Personal Access Token"

    unclassified:
      location:
        url: "https://jenkins.andrei-test.lendo.dev/"

    # --- MOVED TO TOP-LEVEL ROOT ---
    appearance:
      themeManager:
        disableUserThemes: false
        theme: "dark"
    # -------------------------------

    jobs:
      - script: |
          pipelineJob('gcp-app-ci-cd') {
            definition {
              cpsScm {
                scm {
                  git {
                    remote {
                      url('https://github.com/onneyami/gcp-terraform-task-2.git')
                    }
                    branches('*/main')
                  }
                }
                scriptPath('Jenkinsfile')
              }
            }
          }