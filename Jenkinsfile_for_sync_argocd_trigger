pipeline {
    agent any

    environment {
        ARGOCD_APP_NAME = 'wireguard-ui'
        // Explicitly use port 80 (plain HTTP inside cluster network)
        ARGOCD_SERVER   = 'argocd-server.argocd.svc.cluster.local:80'
        ARGOCD_TOKEN    = credentials('argocd-jenkins-token')
    }

    stages {
        stage('Checkout Manifests') {
            steps {
                echo "Pulling latest wireguard-ui manifests from Git..."
                checkout scm
            }
        }

        stage('Trigger ArgoCD Refresh & Sync') {
            steps {
                script {
                    echo "Triggering hard refresh and sync for ${env.ARGOCD_APP_NAME}..."
                    sh '''
                        # 1. Refresh ArgoCD app state via HTTP
                        echo "Refreshing application..."
                        curl -s -f -X GET \
                          -H "Authorization: Bearer ${ARGOCD_TOKEN}" \
                          "http://${ARGOCD_SERVER}/api/v1/applications/${ARGOCD_APP_NAME}?refresh=hard" > /dev/null

                        sleep 2

                        # 2. Trigger Application Sync
                        echo "Triggering sync..."
                        curl -s -f -X POST \
                          -H "Authorization: Bearer ${ARGOCD_TOKEN}" \
                          -H "Content-Type: application/json" \
                          "http://${ARGOCD_SERVER}/api/v1/applications/${ARGOCD_APP_NAME}/sync" \
                          -d '{"revision": "HEAD", "prune": true}'
                    '''
                }
            }
        }
    }

    post {
        success {
            echo "Successfully triggered ArgoCD sync for ${env.ARGOCD_APP_NAME}!"
        }
        failure {
            echo "Failed to trigger ArgoCD sync."
        }
    }
}